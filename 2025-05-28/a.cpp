l : = label.new(x = bar_index - int(sessBars / 2),
                y = hi + syminfo.mintick * 20, text = name,
                style = label.style_label_down, textcolor = color.white,
                color = directionColor)
